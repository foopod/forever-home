name: Deploy

on: [push]

jobs:
  deploy:
    runs-on: self-hosted 

    - name: Deploy to production
      run: |
        echo "Deploying to production"
        docker compose up -d
        echo "Deployed!"